# Production Server Configuration
# REACT_APP_SERVER_URL=https://procomm-server.vercel.app

# Temporarily disabled - using same origin for API calls
# This means API calls will fail until server is deployed
# You need to deploy server separately and uncomment the line above with correct URL